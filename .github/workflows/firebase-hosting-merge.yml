# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
    paths:
    - client/**
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Define variables
        run: echo "Variable are ${REACT_APP_URL_API}"
        env:
          REACT_APP_URL_APP:"https://us-central1-paises-e1544.cloudfunctions.net/api/"
        working-directory: ./client

      - name: Building
        run: npm i && npm run build
        env:
            REACT_APP_URL_APP:"https://us-central1-paises-e1544.cloudfunctions.net/api/"
        working-directory: ./client

      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_PAISES_E1544 }}'
          channelId: live
          projectId: paises-e1544
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
